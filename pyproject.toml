[project]
name = "osage-chan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "zeetee1235", email = "zeetee1235@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "discord>=2.3.2",
    "dotenv>=0.9.9",
    "edge-tts>=7.2.0",
    "jamo>=0.4.1",
    "requests>=2.32.4",
    "yt-dlp>=2025.7.21",
    "PyNaCl>=1.5.0",
]

[project.scripts]
osage-chan = "osage_chan:main"

[build-system]
requires = ["uv_build>=0.8.8,<0.9.0"]
build-backend = "uv_build"
